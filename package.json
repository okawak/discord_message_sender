{
  "name": "discord-message-sender",
  "version": "0.5.4",
  "private": true,
  "description": "Send messages to Obsidian from Discord.",
  "license": "MIT",
  "author": "okawak",
  "type": "module",
  "scripts": {
    "build": "bun run wasm:build && bunx vite build --mode production && bun run copy:manifest",
    "check": "bunx biome check && cargo clippy --workspace --all-targets --locked && cargo clippy --workspace --target wasm32-unknown-unknown --lib --locked",
    "check:fix": "bunx biome check --write",
    "clean": "cargo clean && bun run clean:files",
    "clean:files": "bun -e \"import fs from 'node:fs'; ['dist','dev','pkg','main.js','main.js.map',...await Array.fromAsync(new Bun.Glob('*.wasm').scan('.'))].forEach(p => fs.rmSync(p, {recursive:true, force:true}))\"",
    "copy:manifest": "bun -e \"await Bun.write('dist/manifest.json', Bun.file('manifest.json'))\"",
    "dev:build": "bun run wasm:build && bunx vite build --mode development",
    "release:prepare": "bun scripts/prepare-release.ts",
    "test": "bun run test:ts && bun run test:html && bun run test:rust",
    "test:html": "bun tests/htmlConversion.smoke.ts",
    "test:rust": "cargo test --workspace --locked",
    "test:ts": "bun test",
    "test:wasm": "bun tests/wasm.smoke.ts && bun tests/bundle.smoke.ts",
    "type-check": "bun run wasm:build && tsc --noEmit",
    "verify:build": "bun scripts/verify-build.ts",
    "wasm:build": "bun scripts/build-wasm.ts"
  },
  "dependencies": {
    "parse5": "8.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/bun": "1.4.2",
    "binaryen": "132.0.0",
    "obsidian": "1.13.1",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  },
  "packageManager": "bun@1.4.2",
  "engines": {
    "node": ">=22.12.0"
  }
}
